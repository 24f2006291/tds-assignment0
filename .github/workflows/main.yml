name: TDS Assignment

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 24f2006291@ds.study.iitm.ac.in
        run: echo "Hello World"

      - name: Second Step
        run: echo "GitHub Actions is running"
